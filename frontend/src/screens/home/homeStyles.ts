import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 30,
    marginBottom: 60,
    height: "100%",
  },
  recipeList: {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
  },
});
