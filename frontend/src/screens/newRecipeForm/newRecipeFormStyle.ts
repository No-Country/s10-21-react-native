import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  formContainer: {
    padding: 30,
    gap: 20,
  },
  inputContainer: {},
  errorMessage: {
    color: "red",
    marginTop: 5,
  },
});
